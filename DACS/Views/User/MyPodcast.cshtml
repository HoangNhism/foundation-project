@model DACS.Models.PodcastViewModel
@{
    ViewData["Title"] = "Danh sách Podcast";
}
<div class="d-block d-md-flex podcast-entry bg-white mb-5" data-aos="fade-up">
    @foreach (var item in Model.Podcasts)
    {
        <div class="btn-group" style="width:14%">
            <a asp-controller="User" asp-action="MyEpisodesByPodcast" asp-route-id="@item.PodcastID">
                <button style="background-image: url('/@item.ImageUrl'); background-size: cover; width: 200px; height: 50px; color: white; font-size: 16px; border: none; border-radius: 5px; cursor: pointer;"></button>
            </a>
        </div>
    }
</div>